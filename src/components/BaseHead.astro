---
import { WEBMENTION_PINGBACK, WEBMENTION_URL } from "astro:env/client";
import { siteConfig } from "@/site.config";
import type { SiteMeta } from "@/types";
import "@/styles/global.css";

// Critical font subset preloaded so the browser can fetch in parallel with CSS,
// not after. Latin-only Roboto Mono (normal weight) is the most-used face on
// every page; preloading it shrinks the FOUT window where the metric-matched
// fallback is visible. Other subsets (latin-ext for PT-BR, italic, Newsreader)
// load via the @font-face declarations on demand.
import robotoMonoLatin from "@fontsource-variable/roboto-mono/files/roboto-mono-latin-wght-normal.woff2?url";

type Props = SiteMeta;

const { articleDate, description, ogImage, title } = Astro.props;

const titleSeparator = "-";
const siteTitle = `${title} ${titleSeparator} ${siteConfig.title}`;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const socialImageURL = new URL(ogImage ? ogImage : "/og-image/default.png", Astro.url).href;
---

<meta charset="utf-8" />
<meta content="width=device-width, initial-scale=1.0" name="viewport" />
<title>{siteTitle}</title>

{/* Preload the body voice. Crossorigin is required for fonts. */}
<link as="font" crossorigin href={robotoMonoLatin} rel="preload" type="font/woff2" />

{/* Icons - theme-adaptive favicons (high contrast against tab bg) */}
<link
	href="/favicon-dark.svg"
	media="(prefers-color-scheme: light)"
	rel="icon"
	type="image/svg+xml"
/>
<link
	href="/favicon-light.svg"
	media="(prefers-color-scheme: dark)"
	rel="icon"
	type="image/svg+xml"
/>
<link href="/favicon-dark.png" rel="apple-touch-icon" />
{import.meta.env.PROD && <link href="/manifest.webmanifest" rel="manifest" />}

{/* Canonical URL */}
<link href={canonicalURL} rel="canonical" />

{/* Primary Meta Tags */}
<meta content={siteTitle} name="title" />
<meta content={description} name="description" />
<meta content={siteConfig.author} name="author" />

{/* Open Graph / Facebook */}
<meta content={articleDate ? "article" : "website"} property="og:type" />
<meta content={title} property="og:title" />
<meta content={description} property="og:description" />
<meta content={canonicalURL} property="og:url" />
<meta content={siteConfig.title} property="og:site_name" />
<meta content={siteConfig.ogLocale} property="og:locale" />
<meta content={socialImageURL} property="og:image" />
<meta content="1200" property="og:image:width" />
<meta content="630" property="og:image:height" />
{
	articleDate && (
		<>
			<meta content={siteConfig.author} property="article:author" />
			<meta content={articleDate} property="article:published_time" />
		</>
	)
}

{/* Twitter */}
<meta content="summary_large_image" property="twitter:card" />
<meta content={canonicalURL} property="twitter:url" />
<meta content={title} property="twitter:title" />
<meta content={description} property="twitter:description" />
<meta content={socialImageURL} property="twitter:image" />

{/* Sitemap */}
<link href="/sitemap-index.xml" rel="sitemap" />

{/* RSS auto-discovery */}
<link href="/rss.xml" title="Blog" rel="alternate" type="application/rss+xml" />

{/* Webmentions */}
{
	WEBMENTION_URL && (
		<>
			<link href={WEBMENTION_URL} rel="webmention" />
			{WEBMENTION_PINGBACK && <link href={WEBMENTION_PINGBACK} rel="pingback" />}
		</>
	)
}

<meta content={Astro.generator} name="generator" />
